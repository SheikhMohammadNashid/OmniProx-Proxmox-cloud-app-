services:
  app:
    image: ${DOCKER_USER}/docmind-rag:v${TAG}
    container_name: docmind-rag
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - docmind-network

networks:
  docmind-network:
    driver: bridge
