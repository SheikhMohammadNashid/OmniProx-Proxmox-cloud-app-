services:
  app:
    image: ${DOCKER_USER}/omniprox:v${TAG}
    container_name: omniprox
    restart: always
    ports:
      - "8000:8000"
    networks:
      - docmind-network

networks:
  docmind-network:
    driver: bridge
